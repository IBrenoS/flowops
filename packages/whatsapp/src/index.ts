// @flowops/whatsapp — implementado na Fase 3
// Responsabilidade: Evolution API client (send, receive, normalize payload)
// IMPORTANTE: instanceId é sempre gerado internamente — NUNCA exposto ao usuário final
// O usuário conecta via QR code. Toda configuração de infraestrutura é transparente.
export {}
